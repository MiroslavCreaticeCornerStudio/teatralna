# Copy to .env for local dev, and set the same vars in Vercel (Project Settings
# → Environment Variables). Used by /api/subscribe (Brevo contact proxy).
BREVO_API_KEY=your-brevo-api-key
BREVO_LIST_ID=14
